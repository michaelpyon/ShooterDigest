name: Weekly Digest

on:
  schedule:
    # Every Monday at noon UTC (7am ET / 4am PT)
    - cron: '0 12 * * 1'
  # Allow manual trigger from GitHub Actions UI
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Ingest digest snapshot
        run: python ingest.py

      - name: Render digest from stored snapshot
        run: python render_from_store.py

      - name: Commit and push digest + docs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/ docs/
          git diff --cached --quiet || git commit -m "chore: auto-generate weekly digest [skip ci]"
          git push

      # Railway auto-deploys when this workflow pushes to main (connected via GitHub source).
      # No manual trigger needed here.
